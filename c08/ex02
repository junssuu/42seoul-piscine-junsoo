#ifndef FT_ABS_H
#define FT_ABS_H

#define ABS(Value) ((Value < 0) ? (-Value) : (Value))

#endif

------------------

main.c

#include "ft_abs.h"
#include <stdio.h>

int main(void)
{
  intValue = -1234;
  int a = ABS(Value);
  printf("a[%d]\n",a);
}
